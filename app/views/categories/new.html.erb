<div class="container">
  <div class="new-form">
    <h1>Add a new category:</h1>
      <%= simple_form_for @category do |f| %>
        <%= f.input :name %>
        <%= f.input :photo, as: :file %>
        <%= f.submit %>
      <% end %>
  </div>
  <%= render 'shared/home_link' %>
</div>
